services:
  parser-api:
    build: .
    container_name: parser_sunat_api
    ports:
      - "10022:8000"
    env_file:
      - .env
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G